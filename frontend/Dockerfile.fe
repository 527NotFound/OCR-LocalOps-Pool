# 1. 빌드 스테이지
FROM node:20-alpine AS builder
WORKDIR /app
COPY package.json .
# npm install을 사용하여 의존성 설치
RUN npm install
COPY . .
# React 앱 빌드
RUN npm run build

# 2. 서빙 스테이지 (경량 Nginx 사용)
FROM nginx:alpine
# 빌드된 React 파일을 Nginx의 웹 루트에 복사
COPY --from=builder /app/build /usr/share/nginx/html
# Nginx 설정 파일 복사 (필요 시: 기본 설정은 SPA에 적합)
# COPY nginx.conf /etc/nginx/nginx.conf 
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]