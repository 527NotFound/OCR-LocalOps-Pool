apiVersion: v1
kind: Service
metadata:
  name: worker-c-service
  namespace: ocr-dev
spec:
  # 💡 ClusterIP 타입으로 설정 (클러스터 내부 전용)
  type: ClusterIP 
  selector:
    app: ocr-worker-c
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000