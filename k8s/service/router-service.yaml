apiVersion: v1
kind: Service
metadata:
  name: router-service
  namespace: ocr-dev
spec:
  type: NodePort # 💡 외부에서 UTM VM IP와 NodePort로 접근 가능
  selector:
    app: ocr-router
  ports:
    - protocol: TCP
      port: 8000         # K8s 내부 포트
      targetPort: 8000   # Router Pod의 포트
      nodePort: 30001    # 💡 NodePort 지정 (프론트엔드에서 사용할 포트)